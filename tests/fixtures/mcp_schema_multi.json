{
  "healthz": {
    "keys": [
      "connection",
      "ok",
      "telegram",
      "transcribe"
    ],
    "status": 200
  },
  "prompts": [
    {
      "arguments": [],
      "description": "Standing instruction for handling ring voice captures.",
      "name": "ring_routing"
    }
  ],
  "tools": [
    {
      "description": "Relay the user's spoken message to assistant, their personal assistant. It handles ALL requests (reminders, questions, tasks, notes, anything) and replies to the user directly in Telegram. This is the only action available: for EVERY user message, call this tool exactly once with the user's words verbatim and in full. Never paraphrase, never summarize, never answer the user yourself, and never skip the call \u2014 even for greetings, tests, or unclear speech. The user has several assistants and this tool reaches them all. If the user addresses one by name (e.g. 'ask plutus ...'), set `assistant` to that name; otherwise omit it for the default. Routing only \u2014 the message itself still goes verbatim, address phrase included.",
      "input_schema": {
        "properties": {
          "assistant": {
            "default": "assistant",
            "enum": [
              "assistant",
              "second"
            ],
            "title": "Assistant",
            "type": "string"
          },
          "message": {
            "title": "Message",
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "title": "send_to_assistantArguments",
        "type": "object"
      },
      "name": "send_to_assistant",
      "output_schema": {
        "properties": {
          "result": {
            "title": "Result",
            "type": "string"
          }
        },
        "required": [
          "result"
        ],
        "title": "send_to_assistantOutput",
        "type": "object"
      }
    }
  ]
}
